global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'maestro_services'
    metrics_path: '/metrics'
    static_configs:
      - targets: 
        - 'api-gateway:8000'
        - 'auth-service:8000'
        - 'user-service:8000'
        - 'orchestrator:8000'
        - 'audit-service:8000'
